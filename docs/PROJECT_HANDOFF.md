# PROJECT HANDOFF — Прошивка МКС / Мини-стенд (DW1000 + STM32F411RE)

**Статус документа:** рабочий черновик v0.17 (**исследование дальности §15.1 закрыто
инженерным выводом:** аппаратное ограничение — вероятно, модуль-клон **DWM1000-SMA
YCHIOT** (не оригинал DecaWave) + RF-разводка; все программные средства проверены
замерами и исчерпаны, см. `docs\FINDINGS_range_investigation.md`. Ранее v0.16:
реализован и проверен на железе **канал данных над UWB §15.1, стадии (0)–(1)** —
host-only, транспорт `content=3`:
тело принятого кадра наружу + текстовый чат с фрагментацией между двумя станциями.
Ранее: приёмная диагностика доведена до **полной — RSSI, FP_POWER и SNR считаются в
прошивке** (строго, формулы UM §4.7 / DecaRanging на float); регулировка мощности TX
(`SET_TX_POWER`), приём и передача, включая периодическую
(`TX_FRAME`/`TX_PERIODIC`/`TX_STOP`), `SET_PHY_CONFIG` + приёмный тракт против EVK1000
Mode 3, слой протокола PING/INIT/GET_STATUS, устранение зависания INIT в USB-ISR)
**Назначение:** единая согласованная точка входа в проект. Содержит сверку
требований из всех исходных документов, зафиксированные решения, выявленные
противоречия и открытые вопросы (TBD). Обновляется по мере поступления данных.

---

## 1. Краткое описание проекта

Разработка встроенного ПО (прошивки) для двух конструктивно различных плат на
одном микроконтроллере **STM32F411RE**:

- **МКС** (Модуль коммуникации и сопряжения) — плата расширения ПК, **2× DWM1000**
  (Источник СШП = эталонный TX, Индикатор СШП = эталонный RX).
- **Мини-стенд** — отдельная плата, **1× DWM1000**.

Связь с ПК (MATLAB) — через **USB CDC (Virtual COM Port)**, бинарный протокол с
CRC8. МК принимает команды верхнего уровня (API v1.3), транслирует их в операции
с регистрами DW1000, собирает диагностику (RSSI, SNR, CIR, метки времени).
Задача НИР — обнаружение СШП/UWB-сигналов.

> **Роль МКС (важно для приоритетов).** МКС — **вспомогательный измерительный
> инструмент** для исследователей, которые разрабатывают алгоритмы детектирования
> СШП **в MATLAB**: его дело — давать чистые данные (метрики, форма CIR) и
> управление радио по USB CDC. **Сам обнаружитель — отдельное будущее устройство**
> (макет: **5× DWM1000** по одному на канал по стандарту, двухъядерный ЦП +
> графический экран); продуктовый детектор в МКС НЕ реализуется. GUI-«лампа
> присутствия» и подобное — проба части функционала будущего обнаружителя, не цель
> МКС. Приоритет развития МКС — качество как прибора: MATLAB-хост, потоковый CIR,
> надёжность (24ч/IWDG/EXTI).

**ОС:** FreeRTOS. **HAL:** STM32 HAL. **Низкий уровень DW1000:** официальный
DecaDriver v4.0.6 (см. раздел 5).

---

## 2. Целевое железо

| Параметр | Значение | Источник / примечание |
|---|---|---|
| МК | **STM32F411RE** (Cortex-M4F) | Указан пользователем. **Заменяет** STM32F405RGT6 из документов |
| RAM / Flash | 128 КБ / 512 КБ | F411RE. Достаточно (буфер CIR 4064 Б + очереди + USB) |
| USB | USB OTG FS → CDC | Требует точные 48 МГц |
| HSE (кварц) | МКС: **24 МГц**, Мини-стенд: **8 МГц** | Подтверждено (схема + Nucleo). Разные настройки PLL для 48 МГц USB |
| DW1000 кварц | 38.4 МГц | На модулях DWM1000 (уже установлен) |
| SPI к радио | МКС: **раздельные** SPI2(M1)+SPI3(M2); Мини-стенд: SPI1 | До 20 МГц (после init); медленный (<3 МГц) на этапе init. **НЕ общая шина** (см. §3 п.7) |
| Питание | 3.3 В (LM1117-3.3) | Развязка по питанию на каждый модуль |
| Watchdog | IWDG обязателен | ТЗ п.4.2 |

### 2.1 Различия плат (вынести в `board_config.h`)

| Аспект | МКС | Мини-стенд (Nucleo-F411RE) |
|---|---|---|
| Число DWM1000 | 2 | 1 |
| `DWT_NUM_DW_DEV` | 2 | 1 |
| Роли | M1 + M2 (Источник/Индикатор назначаются программно) | 1 модуль (для студентов достаточно) |
| Шина(ы) SPI к радио | **раздельные:** M1=SPI2, M2=SPI3 | SPI1 |
| HSE кварц | **24 МГц** (XT1) | 8 МГц |
| Внешняя EEPROM | **M95080 на SPI1** (CS=PA4) | нет |
| Макрос сборки | `BOARD_MKS` | `BOARD_MINISTEND` |

#### Распиновка МКС (из схемы PCI_E_PCB) — ПОДТВЕРЖДЕНО

| Сигнал | STM32F411 | Модуль | Примечание |
|---|---|---|---|
| SPI2 SCK / MISO / MOSI | PB10 / PC2 / PC3 | M1 | шина радио №1 |
| SPI2 CS (NSS) | PB12 | M1 | CS1 |
| SPI3 SCK / MISO / MOSI | PC10 / PC11 / PC12 | M2 | шина радио №2 |
| SPI3 CS (NSS) | PA15 | M2 | CS2 |
| IRQ1 | PB8 | **M2** GPIO8 | внимание: IRQ1↔M2 |
| IRQ2 | PB9 | **M1** GPIO8 | внимание: IRQ2↔M1 |
| RST1 | PB6 → VT1 (BSS138) | M1 RSTn | сброс через N-MOSFET (инверсия уровня) |
| RST2 | PB7 → VT2 (BSS138) | M2 RSTn | сброс через N-MOSFET (инверсия уровня) |
| SPI1 SCK/MISO/MOSI/NSS | PA5/PA6/PA7/PA4 | M95080 EEPROM | НЕ радио |
| USB DM / DP | PA11 / PA12 | XP1 | USB FS (CDC) |
| LED | PB-линии через R3/R5/R6/R8/R9 | — | TX-красный, RX-жёлтый |

> **Внимание (нумерация IRQ):** на схеме IRQ1 идёт к модулю M2, а IRQ2 — к M1.
> Перекрёстное именование. В `board_config.h` именовать по модулю, не по номеру
> цепи, чтобы не путать.

> **Сброс через MOSFET:** RST управляется N-канальным BSS138 → логика
> **инвертирована** относительно прямого подключения. Это нужно учесть в функции
> аппаратного сброса (активный уровень). Проверить на железе.

#### Распиновка Мини-стенда (Nucleo-F411RE) — ПОДТВЕРЖДЕНО

Один DWM1000 на **SPI1**: CS=PA4, SCK=PB3, MISO=PB4, MOSI=PB5, IRQ=PB0, RST=PC0.

---

## 3. Зафиксированные решения

1. **CRC8 — область покрытия:** считается по **LEN + CMD_ID + PARAMS (без SYNC)**,
   как в ТЗ п.5.2.2. Алгоритм: CRC-8, полином `0x07`, init `0x00`, без рефлексии
   и xorout. Ответ: CRC по **LEN + STATUS + DATA**.
   - **РЕШЕНО (OQ-1 закрыт):** делаем «как правильно» — CRC **без SYNC**. Пример
     MATLAB-кода в документе протокола (где CRC считается по всему массиву с
     SYNC) **игнорируем как ошибочный**. Нужно выдать заказчику **исправленный
     пример MATLAB** (см. раздел 12).
   - Область покрытия всё равно оставить параметром/макросом — дёшево и страхует.
2. **Несовпадение CRC →** вернуть код ошибки (`INTERNAL_ERROR` по ТЗ), не «молча
   игнорировать».
3. **Терминология двух модулей:** Источник (TX) / Индикатор (RX). Унифицировать в
   коде (`DEV_SOURCE` / `DEV_INDICATOR` или `DEV_TX` / `DEV_RX`).
4. **PING** добавлен в v1.3 позже → расхождение «16 vs 17 команд» закрыто, это
   версионный артефакт. Канон — таблицы протокола/API v1.3 (CMD_ID 0x00–0x61).
5. **Низкий уровень DW1000:** берём официальный **DecaDriver v4.0.6**, самодельный
   `dw1000_driver.c` **не используем**. Из самодельной библиотеки сохраняем
   верхние уровни (см. раздел 6).
6. **МК:** STM32F411RE для обеих плат. STM32F405RGT6 из документов — устаревшая
   информация.
7. **SPI к радио — РАЗДЕЛЬНЫЕ шины (КОРРЕКЦИЯ).** Схема МКС показывает: M1 на
   **SPI2**, M2 на **SPI3** (а SPI1 занят EEPROM M95080). Это противоречит всем
   текстовым документам, где SPI назывался «общим». Верить схеме.
   - Мьютекс «на общую шину» в прежнем виде не нужен; чипы аппаратно независимы.
   - Но `pdw1000local` в DecaDriver — глобальный. Защищаем пару
     «`dwt_setlocaldataptr(idx)` + транзакция» (мьютекс) ИЛИ держим всю работу с
     радио в одной `Radio_Manager_Task` (тогда гонок нет по построению).
   - `deca_spi.c` выбирает И нужный `SPI_HandleTypeDef` (hspi2/hspi3), И линию CS
     по активному устройству.
8. **Сброс DWM1000 через N-MOSFET (BSS138).** Линия RST инвертирована. Учесть
   активный уровень в функции аппаратного сброса. Проверить на железе.
9. **USB-роли (OQ-6 закрыт):** «внешний USB» (XP1/Host-разъём) — связь с ПК
   (MATLAB, протокол + CDC). Отладочный USB на плате — подключение к дебаггеру
   STM32CubeIDE; **DEBUG Console идёт через него** (а не через отдельный UART, как
   предполагалось в самодельной библиотеке). Уточнить транспорт консоли:
   ST-LINK VCP/UART или отдельный CDC. См. OQ-8.

---

## 4. Сверка требований: команды API (канонический чек-лист)

Источники: «Бинарный протокол МКС v1.3», «МКС API v1.3», ТЗ п.5.3, DW1000 UM.
Колонка «Параметры» — формат на проводе (little-endian). Колонка «Регистры/действия»
— что прошивка делает с DW1000 (через DecaDriver).

| CMD_ID | Команда | Параметры (wire) | Ответ DATA | Регистры/действия DW1000 | Примечание |
|---|---|---|---|---|---|
| 0x00 | PING | — | OK uint8 (PONG=0x00) | нет | Немедленный ответ, без обращения к чипу |
| 0x01 | INIT | — | — | DEV_ID, PMSC_CTRL0, OTP_CTRL, SYS_CFG, AGC_TUNE1/2/3, DRX_TUNE2 | `dwt_initialise()` + загрузка LDE + дефолтная конфигурация + фикс suboptimal (UM §2.5.5) |
| 0x02 | GET_STATUS | — | TX_state u8, RX_state u8, channel u8, data_rate u8, preamble_length u16, PRF u8 | SYS_STATE, SYS_CFG (или из кэша конфигурации) | Брать из кэша прошивки |
| 0x03 | RESET_RADIO | — | — | PMSC_CTRL0 (SOFTRESET) | `dwt_softreset()`, затем восстановить конфигурацию |
| 0x10 | SET_PHY_CONFIG | channel u8, data_rate u8, preamble_length u16, preamble_code u8, PRF u8, PAC_size u8 | — | CHAN_CTRL, TX_FCTRL, DRX_TUNE0b/1a/2, AGC_TUNE1 | Заполняет `dwt_config_t` → `dwt_configure()`. **Нужна трансляция** (см. 4.1) |
| 0x11 | SET_TX_POWER | power_level u8 | — | TX_POWER | Маппинг u8 → значение TX_POWER (UM рис.26). Валидация диапазона |
| 0x20 | TX_FRAME | length u16, payload[length] | — | TX_BUFFER, TX_FCTRL, SYS_CTRL, SYS_STATUS | `dwt_writetxdata()`+`dwt_writetxfctrl()`+`dwt_starttx()`; ждать TXFRS |
| 0x21 | TX_PERIODIC | period_ms u16, length u16, payload[length] | — | TX_BUFFER, SYS_CTRL | Аппаратный таймер МК → периодический TX_FRAME |
| 0x22 | TX_STOP | — | — | SYS_CTRL (TRXOFF) | Стоп таймера + `dwt_forcetrxoff()` |
| 0x30 | RX_START | — | — | SYS_CTRL (RXENAB), DRX_PRETOC, RX_FWTO | `dwt_rxenable()` |
| 0x31 | RX_STOP | — | — | SYS_CTRL (TRXOFF) | `dwt_forcetrxoff()` |
| 0x40 | GET_SIGNAL_METRICS | — | RSSI i16 (dBm×100), SNR i16 (dB×100), RXPACC u16, FP_INDEX u16 | RX_FQUAL, RX_FINFO, AGC_STAT1 | Формулы RSSI/SNR — UM §4.7.1/4.7.2 |
| 0x41 | GET_CIR | offset u16, length u16 | I/Q пары int16 (N=length) | ACC_MEM, PMSC_CTRL0 | При чтении ACC_MEM отбросить 1-й dummy-байт. Макс. 1016 отсчётов |
| 0x50 | START_EXPERIMENT | — | поток: timestamp, RSSI, SNR, RXPACC | RX_FQUAL, RX_FINFO | Потоковая телеметрия по приёму пакетов |
| 0x51 | STOP_EXPERIMENT | — | — | — | Снять флаг потокового режима |
| 0x60 | TX_SWEEP | channel_start u8, channel_end u8, power_start u8, power_end u8, preamble_length u16 | результаты измерений RX | CHAN_CTRL, TX_POWER, TX_FCTRL | Перебор параметров + передача |
| 0x61 | DETECTOR_TEST | number_of_packets u16, tx_power_start u8, tx_power_end u8 | P_detect, P_false, RSSI, SNR | RX_FQUAL, RX_FINFO, ACC_MEM | Серия передач + статистика, ROC |

### 4.1 Важная трансляция параметров (wire → DecaDriver)

Протокол передаёт `preamble_length` как «сырое» число (u16, напр. 128), а
`dwt_config_t` ожидает **enum** `DWT_PLEN_64..DWT_PLEN_4096`. Аналогично
`data_rate`, `PRF`, `PAC` — на проводе это малые числа/коды, в драйвере —
именованные константы (`DWT_BR_110K/850K/6M8`, `DWT_PRF_16M/64M`,
`DWT_PAC8/16/32/64`). **Конфигуратор PHY обязан содержать таблицы соответствия и
валидацию** (UM Таблицы 16, 61). Невалидное значение → `INVALID_PARAM`.

### 4.2 Коды STATUS (полный набор, ТЗ п.5.2.4)

`0x00 OK` · `0x01 UNKNOWN_CMD` · `0x02 INVALID_PARAM` · `0x03 RADIO_BUSY` ·
`0x04 RADIO_ERROR` · `0x05 BUFFER_OVERFLOW` · `0x06 TIMEOUT` · `0x07 INTERNAL_ERROR`

### 4.3 Флаги ошибок DW1000 для контроля (SYS_STATUS, UM §7.2.17)

`TXFRS` (TX успех), `TXBERR` (ошибка буфера TX), `RXFCG/RXFCE` (приём ок/ошибка
CRC), `RXPHE` (ошибка PHR), `RXOVRR` (переполнение RX), `AFFREJ` (отклонение
фильтром кадров). При `RADIO_ERROR` → вернуть статус + receiver-only reset
(SOFTRESET в PMSC_CTRL0, UM §4.1.6).

---

## 5. Решение по драйверу (DecaDriver v4.0.6)

**Источник:** `dwm1001-examples-master/deca_driver/` (версия `0x040006`).
Остальные репозитории — `uwb-dw1000`, `uwb-core`, `uwb-apps` — это **другой стек**
(uwb-core поверх Apache Mynewt). Для нашего проекта (FreeRTOS + HAL) **не
используются**, остаются как справочный материал.

**Файлы драйвера (берём как есть):** `deca_device.c`, `deca_device_api.h`,
`deca_regs.h`, `deca_params_init.c`, `deca_param_types.h`, `deca_types.h`,
`deca_range_tables.c`, `deca_version.h`.

### 5.1 Поддержка нескольких чипов — решена «из коробки»

Драйвер хранит состояние в **массиве экземпляров**, а не в одной глобальной
переменной:

```c
static dwt_local_data_t dw1000local[DWT_NUM_DW_DEV];
static dwt_local_data_t *pdw1000local = dw1000local;
int dwt_setlocaldataptr(unsigned int index); // переключает активный экземпляр
```

По умолчанию `#define DWT_NUM_DW_DEV (1)` в `deca_device_api.h`.

**Что делаем:**
- МКС: `DWT_NUM_DW_DEV = 2`; Мини-стенд: `= 1`.
- Перед каждой транзакцией с конкретным чипом: под мьютексом SPI выбрать CS
  нужного модуля **и** вызвать `dwt_setlocaldataptr(idx)`. Паттерн доступа:
  `mutex_lock → set CS + dwt_setlocaldataptr(idx) → транзакция → mutex_unlock`.
- Вся сериализация — в `Radio_Manager_Task` (единственный владелец SPI к радио).

### 5.2 Платформенный слой — пишем сами (под STM32F411 / HAL)

DecaDriver платформенно-независим. Реализуем:
- `deca_spi.c` — `writetospi()` / `readfromspi()` через `HAL_SPI_TransmitReceive`.
  **На МКС выбирает И нужный SPI (hspi2 для M1 / hspi3 для M2), И линию CS** по
  активному устройству. На Мини-стенде — всегда hspi1. Текущее устройство берётся
  из `board_config` по индексу, синхронно с `dwt_setlocaldataptr(idx)`.
- `deca_sleep.c` — `deca_sleep()` (мс-задержка).
- `decamutexon()` / `decamutexoff()` — критическая секция / мьютекс FreeRTOS
  (защищает глобальный `pdw1000local`, см. §3 п.7).
- Аппаратный сброс по линии RST каждого модуля. **На МКС RST через BSS138 →
  инверсия уровня** (§3 п.8).
- EXTI на линиях IRQ → разблокировка `Radio_Manager_Task`
  (`xSemaphoreGiveFromISR` / `xQueueSendFromISR`). На МКС внимание к перекрёстной
  нумерации: IRQ1↔M2, IRQ2↔M1.

> Папка `deca_driver/port/` в примере — порт под Nordic nRF52 (плата DWM1001).
> Используем как **референс**, переписываем под STM32F4.

### 5.3 Исправленный баг vendor-драйвера (ЕДИНСТВЕННАЯ правка в decadriver/)

Общее правило — **не модифицировать `Drivers/decadriver/`**. Сделано одно
санкционированное исключение из-за бага в самом vendor-коде.

- **Файл/функция:** `Drivers/decadriver/deca_device.c`, `dwt_setlocaldataptr()`.
- **Баг:** инвертированная проверка границ индекса —
  `if (DWT_NUM_DW_DEV > index)`. Условие истинно как раз когда индекс **внутри**
  границ, поэтому функция возвращала `DWT_ERROR` для любого валидного индекса
  (в т.ч. 0 и 1). Комментарий в коде и описание API говорят обратное.
- **Последствие:** ломался выбор устройства — `deca_port_select_device()`
  получал ошибку и `bringup_read_devids()` пропускал оба модуля; DEV_ID не читался.
- **Исправление:** заменено на `if (index >= DWT_NUM_DW_DEV)`. Исходная строка
  сохранена рядом в комментарии, над функцией — маркер `NCPR FIX (2026-07-06)`.
- **ВАЖНО при обновлении DecaDriver:** правку нужно перенести заново — новая
  версия vendor-файла её затрёт. Искать по маркеру `NCPR FIX`.

---

## 6. Что берём из самодельной библиотеки (verbatim/с правками)

| Компонент | Решение |
|---|---|
| Уровень протокола (`protocol.c/.h`): парсер, CRC8, диспетчер, построитель ответов | **Сохранить**, адаптировать под DecaDriver |
| Отладочная консоль (`debug_console.c/.h`) | **Только мини-стенд**, USART2 (huart2, PA2/PA3, ST-LINK VCP). На МКС консоли нет — всё через MATLAB |
| Хранилище настроек (M95080 EEPROM, SPI1) | **Новый модуль** `settings_storage` (antenna delay, PHY-профиль, серийник; сигнатура+CRC). Только МКС |
| USB CDC (`usbd_cdc_if.c/.h`), кольцевой буфер | **Сохранить** |
| Структура задач FreeRTOS, очереди/семафоры | **Сохранить** (см. раздел 8) |
| `dw1000_driver.c/.h` (низкий уровень) | **Заменить** на DecaDriver v4.0.6 |
| `radio_manager.c/.h` | **Переписать** под вызовы DecaDriver |

---

## 7. Открытые вопросы (Open Questions)

**Закрыто:**
- ~~OQ-1 (CRC area)~~ → CRC **без SYNC**, MATLAB-пример из документа считаем
  ошибочным, выдаём исправленный (раздел 12).
- ~~OQ-2 (Pinout МКС)~~ → извлечён из схемы (см. §2.1).
- ~~OQ-3 (Pinout Мини-стенд)~~ → SPI1: CS-PA4 SCK-PB3 MISO-PB4 MOSI-PB5 IRQ-PB0 RST-PC0.
- ~~OQ-4 (Роль модуля Мини-стенда)~~ → один модуль, для учебных задач достаточно.
- ~~OQ-5 (HSE кварц)~~ → МКС 24 МГц, Мини-стенд 8 МГц.
- ~~OQ-6 (USB Host)~~ → внешний USB = MATLAB/протокол; отладочный USB = дебаггер +
  DEBUG Console.

**Закрыто (раунд 2):**
- ~~OQ-7 (EEPROM)~~ → M95080 (SPI1) = хранилище настроек: antenna delay, PHY-профиль,
  серийник. Реализуем модуль `settings_storage` (сигнатура + CRC). Закрывает ТЗ п.5.5.
- ~~OQ-8 (DEBUG Console)~~ → через **ST-LINK VCP = USART2 (PA2/PA3)**, не отдельный
  UART/CDC. Самодельную консоль перецепить на huart2.
- ~~OQ-10 (роли M1/M2)~~ → **фиксированные**: Источник (TX тестовых сигналов) +
  Индикатор (подтверждение наличия сигнала в эфире). Привязка статическая в
  `board_config.h`. Соответствие «M1/M2 ↔ роль» уточнить на железе при bring-up
  (если перепутано — поменять одной строкой). LED: TX-красный, RX-жёлтый.

- ~~OQ-12 (Консоль на МКС)~~ → **МКС без физической DEBUG Console.** Всё
  управление/диагностика — через MATLAB (USB CDC). Отладка МКС — через SWD
  (опц. printf по ITM/SWO). Физическая консоль (USART2 PA2/PA3, ST-LINK VCP) —
  только на мини-стенде.

**Закрыто (раунд 3):**
- ~~OQ-9 (Активный уровень RST через BSS138)~~ → **подтверждён на железе.**
  `DW_RST_ACTIVE_HIGH=1` верно (лог.1 на GPIO МК → MOSFET открыт → RSTn к земле →
  **сброс активен высоким**). Гипотеза подтверждена практикой: с этой полярностью
  оба чипа отвечают `0xDECA0130`; при неверной полярности сброс не снимался бы и
  чипы не ответили бы.

**Открыто:**
- **OQ-11 (MATLAB-скрипт заказчика):** желателен как эталон поведения протокола.

---

## 8. Структура задач FreeRTOS (целевая)

| Задача | Приоритет | Функция |
|---|---|---|
| USB_Command_Task | 2 | Приём байт из USB CDC, сборка пакета, CRC, декод, → очередь команд |
| Radio_Manager_Task | 3 | Владелец SPI к радио. Исполняет команды, обрабатывает IRQ-события обоих чипов |
| Periodic_TX_Task | 2 | TX_PERIODIC по таймеру (`vTaskDelayUntil`) |
| Diagnostic_Stream_Task | 1 | Потоковая телеметрия в режиме START_EXPERIMENT |
| USB_Transmit_Task | 2 | Отправка буферов из `xUSB_TxQueue` в USB CDC |
| DEBUG_Console_Task | 1 | Отладочная консоль UART (115200 8N1) |

Очереди: `xRadioCommandQueue`, `xRadioEventQueue`, `xUSB_TxQueue`,
`xConsoleCommandQueue`. Семафоры: `xTxCompleteSemaphore`, `xRxCompleteSemaphore`.

---

## 9. Целевая архитектура прошивки (слои)

```
МКС/Мини-стенд (прошивка)
├── Protocol Layer        — парсер, CRC8, диспетчер CMD_ID, построитель ответов
├── DW1000 Control Layer  — обёртка над DecaDriver:
│   ├── INIT / RESET_RADIO
│   ├── PHY-конфигуратор (SET_PHY_CONFIG/SET_TX_POWER) + трансляция wire→enum
│   ├── TX (TX_FRAME/TX_PERIODIC/TX_STOP)
│   ├── RX (RX_START/RX_STOP)
│   └── Диагностика (GET_SIGNAL_METRICS/GET_CIR)
├── Experiment Layer      — START_EXPERIMENT, TX_SWEEP, DETECTOR_TEST
├── Settings Storage      — M95080 EEPROM (SPI1): antenna delay, PHY-профиль, серийник [только МКС]
├── DecaDriver v4.0.6     — фирменный низкий уровень (НЕ модифицируем)
├── Platform port         — deca_spi/deca_sleep/mutex/RST/EXTI (наш, под F411)
└── System Layer          — FreeRTOS, таймеры, IWDG, USB CDC, board_config.h
```

---

## 10. Тайминги и требования надёжности (ТЗ)

- Отклик: ≤100 мс (конфигурация), ≤10 мс (команды управления передачей).
- IWDG обязателен; нет «зависаний».
- 24 ч непрерывной работы TX/RX без сбоев (критерий приёмки).
- Загрузка LDE-микрокода при INIT и после пробуждения из SLEEP — до включения RX
  (нужно для точных меток времени, UM Table 4).
- Delayed TX/RX через `DX_TIME` — для точного управления временем.

---

## 11. Следующие шаги

1. ~~Распиновка обеих плат~~ → получена (МКС из схемы, Nucleo задан).
2. ~~Каркас платформенного слоя~~ → `deca_port.c/.h` готов; проект bring-up
   **собирается чисто (Debug)**: deca_port + bringup + DecaDriver 4.0.6.
   protocol.*/debug_console.* временно исключены из сборки (Exclude from Build).
   Работаем на плате **МКС** (мини-стенд отложен, платы нет на руках).
3. ~~КЗ NRST~~ → устранено (кнопка BT1, см. §13). Аппаратный сброс восстановлен.
4. ~~Прочитать `DEV_ID`~~ → ✅ **ВЫПОЛНЕНО.** Оба модуля ответили `0xDECA0130`
   (M1 на SPI2, M2 на SPI3); `g_devid[0]` и `g_devid[1]` = `0xDECA0130`, читано
   через отладку. Подтверждено: с mini-ST-LINK не читалось (`0xFFFFFFFF`) — нехватка
   питания; с более мощным ST-LINK / внешним 3.3В читается стабильно (см. §14.4).
   Полярность RST подтверждена практикой (`DW_RST_ACTIVE_HIGH=1`, см. OQ-9).
   Bring-up завершён — SPI-связь с обоими чипами доказана.
5. ~~Слой протокола (PING/INIT/GET_STATUS)~~ → ✅ **ВЫПОЛНЕНО и проверено на
   железе (2026-07-16).** Ядро протокола развязано от удалённого `dw1000_driver`
   и RTOS (bare-metal); реализованы 3 системные команды поверх DecaDriver через
   `deca_port`. Проверено end-to-end по USB CDC (COM3) хост-скриптами
   `tools/mks_*_test.py`: PING→PONG (DATA=0x00), INIT→OK (оба модуля DWM1000
   инициализированы), GET_STATUS→7 байт. Спека — `docs/PROTOCOL_SPEC.md`.
   - **Важная находка (архитектура):** INIT зависал, т.к. обработка команд шла
     в контексте USB-прерывания, а `dwt_initialise`/`hard_reset` вызывают
     `HAL_Delay`, который в ISR виснет (USB IRQ приоритет 0 > SysTick 15 → `uwTick`
     заморожен). Решено переносом разбора в main loop через RX-кольцевой буфер
     (ISR только копирует байты). См. `docs/REPORT_init_hang_usb_isr_diagnosis.md`.
     Паттерн «короткий ISR, тяжёлое в фоне» распространить на будущие TX/RX.
6. ~~Конфигурация PHY (`SET_PHY_CONFIG`, 0x10)~~ → ✅ **ВЫПОЛНЕНО.** Трансляция
   wire→enum DecaDriver (§4.1) с валидацией; авто-nsSFD по data_rate; применяется
   на все модули; требует предварительного INIT. Использовано в проверке приёма
   (INIT→SET_PHY_CONFIG(Mode3)→RX_START). `SET_TX_POWER` (0x11) — в TX-заходе.
7. ~~Приёмный тракт (`RX_START` 0x30 / `RX_STOP` 0x31 / `GET_SIGNAL_METRICS` 0x40)~~
   → ✅ **ВЫПОЛНЕНО и проверено на железе (2026-07-16).** Реализованы поверх
   DecaDriver: непрерывный приём (`dwt_rxenable`), обслуживание в main loop
   (`PROTOCOL_PollRadio`, опрос SYS_STATUS: RXFCG→метрики через
   `dwt_readdiagnostics`, ошибки→`dwt_rxreset`), интерим-метрики (сырые поля
   `dwt_rxdiag_t`). Проверено против эталонного передатчика **EVK1000 (Mode 3,
   0.7 м LOS)** через `tools/mks_console.py`: растущий `count` (48→81→138),
   физичные метрики (CIR_PWR ~9000, RXPACC ~270, FP_AMPL1..3 ~16000–18000).
   Слушающий модуль M2 расслышал EVK — свап на M1 не понадобился. Это выполнение
   **`PLAN_rx_tract` §10 шаг 5**.
   - **RX теперь доверенный узел:** можно проверять будущий TX через петлю
     M1(TX)→M2(RX) внутри МКС, не завися от внешнего EVK.
   - ✅ **Хостовая верификация формул мощности UM §4.7 (2026-07-17).** В
     `tools/mks_protocol.py` (`estimate_power`) / `mks_console.py` реализованы
     приближённые `RX_LEVEL`(RSSI)/`FP_POWER`/`diff` + классификатор LOS/gray/NLOS
     (пороги UM §4.7.1). Проверено на живом EVK Mode 3: RX_LEVEL стабилен
     −80…−82 dBm, diff реагирует на смену канала (1.3…8.9 дБ). Расчёт
     **приближённый** (N без SFD-коррекции — нет `RXPACC_NOSAT`). **В код прошивки
     формулы НЕ внесены.** Строгий RSSI/SNR + перенос в прошивку — будущий шаг
     после добавления чтения `RXPACC_NOSAT`.
8. ~~Передающий тракт (`TX_FRAME` 0x20 / `TX_STOP` 0x22)~~ → ✅ **ВЫПОЛНЕНО и
   проверено на железе (2026-07-17).** `TX_FRAME` шлёт одиночный кадр с
   `DW_TX_SOURCE_DEV` (M1): `dwt_writetxdata`/`writetxfctrl`(`length+2` под
   авто-FCS)/`starttx` + busy-wait TXFRS с guard; `TX_STOP` — `dwt_forcetrxoff`.
   Подтверждено внутренним loopback **M1→эфир→M2** при выключенном EVK
   (изолированный тест): пустой эфир→`metrics=TIMEOUT`; после
   `txframe DE AD BE EF 01`→`count=1` (ровно один кадр на один посланный),
   метрики физичные (CIR_PWR ~10450, RXPACC 264, RX_LEVEL −78.8 dBm, diff 2.2 дБ →
   LOS). Выполнение **`PLAN_tx_tract`**. PHY согласован через `mode3` на оба модуля.
   - **Передающий узел теперь доверенный:** есть замкнутая пара свой TX (M1) ↔
     свой RX (M2) — можно проверять будущие команды, не завися от внешнего EVK.
   - **Наблюдение:** loopback шумнее эфира с EVK (STD_NOISE ~164 vs ~56–84) —
     наводки на плате/кабели; учесть при строгом SNR (см. `docs/PROTOCOL_SPEC.md`).
9. ~~Периодическая передача (`TX_PERIODIC` 0x21)~~ → ✅ **ВЫПОЛНЕНО и проверено на
   железе (2026-07-17).** Механизм — **программный интервал по `HAL_GetTick()` в
   main loop** (`PROTOCOL_PollTx`, без аппаратного TIM/CubeMX). Команда только
   взводит режим и сразу возвращает OK; реальная посылка — в main loop тем же
   паттерном, что `TX_FRAME`. `TX_STOP` расширен — снимает и периодику (единая
   точка останова передатчика). Проверено loopback M1→M2: при
   `txperiodic 100 DE AD BE EF 01` `count` растёт (~11 кадров/интервал при 100 мс),
   после `txstop` `count` застыл (два подряд `metrics` идентичны), метрики физичны
   (RX_LEVEL ≈ −79 dBm, diff ≈ 2 дБ → LOS). Хост: `tx_periodic()` в
   `mks_protocol.py` (v3) + команда `txperiodic` в `mks_console.py`. Коммит
   прошивки — `ee1abee`. Выполнение `PLAN_tx_periodic` / `TASK_tx_periodic2`.
10. ~~Регулировка мощности TX (`SET_TX_POWER` 0x11)~~ → ✅ **ВЫПОЛНЕНО и проверено
    на железе (2026-07-17).** Вариант A (ручной режим: `dwt_setsmarttxpower(0)` +
    `dwt_configuretxrf`). `power_level u8` — **больше level → больше мощность**
    (реализация `octet = 0xFF - level` ×4 октета; 0 ≈ мин, `POWER_LEVEL_MAX=0xDF` ≈
    макс; шаг ≈ 0.5 dB). PGdly — по каналу из `TC_PGDELAY_CH*` (deca_regs.h);
    применяется на `DW_TX_SOURCE_DEV`; требует предварительного `SET_PHY_CONFIG`;
    ответ DATA = применённый регистр `power` (u32 LE). Проверено loopback M1→M2:
    `RX_LEVEL` монотонно растёт с level (−110 → −89 → −82 dBm при level 0/60/120).
    Коммиты прошивки: `97a8d8f` (реализация) + `8789eab` (комментарии). Хост:
    `set_tx_power()` в `mks_protocol.py` (v4) + команда `txpower` в `mks_console.py`.
11. ~~Строгий RSSI/FP_POWER в прошивке~~ → ✅ **ВЫПОЛНЕНО и проверено на железе
    (2026-07-17).** Добавлено чтение `RXPACC_NOSAT` (DRX_CONF 0x27, sub-offset 0x2C,
    UM §7.2.40.12) через `dwt_read16bitoffsetreg`; SFD-коррекция N по правилу UM
    стр.96 (коррекция только при `RXPACC == RXPACC_NOSAT`; `SFD_CORRECTION_MODE3=82`
    из UM Table 18); `RX_LEVEL`/`FP_POWER` считаются **в прошивке на float** (`log10f`,
    формулы UM §4.7). `GET_SIGNAL_METRICS` расширен **18 → 28 байт** (первые 18 =
    интерим-совместимость; дописаны RXPACC_NOSAT u16, N_corrected u16, RSSI
    i16 dBm×100, FP_POWER i16 dBm×100, A_used u16). Проверка (loopback M1→M2):
    RXPACC_NOSAT ≈ 1042–1043 (≠ RXPACC ~460–540 → коррекция не нужна, подтверждает
    правило UM); RSSI(fw) сверен с хостовым `estimate_power` — **Δ=0.00 dB**.
    **Требует линковки `-lm`.** Коммит прошивки `5f87d28`. Хост: `parse_signal_metrics`
    v5 (18/28 байт по длине) + сверка хост↔прошивка в `mks_console`.
12. ~~Total SNR в прошивке~~ → ✅ **ВЫПОЛНЕНО и проверено на железе (2026-07-17).**
    `total SNR = RSL + δ`, где δ — из DecaRanging `instance_log.c` (δ=79.5 для
    каналов 1/2/3/5, δ=77.0 для 4/7; знак **ПЛЮС**, подтверждён кодом — не «минус»
    как на форуме). RSL — уже строгий (через `RXPACC_NOSAT`). Попутно исправлена
    константа **A(PRF16) 113.77 → 115.72** под первоисточник DecaRanging (alpha
    PRF16 = −115.72); A(PRF64)=121.74 без изменений. `GET_SIGNAL_METRICS` расширен
    **28 → 30 байт** (SNR i16 dB×100 в конце). Проверка (loopback M1→M2, Mode 3):
    SNR = RSL + 79.5, значения −0.44…0.00 dB при RSL ≈ −79.9…−79.5 dBm —
    согласуется с TotSNR DecaRanging при таких уровнях. Коммит прошивки `64a51e2`.
    Хост: `parse_signal_metrics` v5 (18/28/30 байт), печать SNR, баннер с версией
    и «(c) 2026 NCPR, Flexlab LLC».
    - **Приёмная диагностика (RSSI/FP_POWER/SNR) полностью замкнута** — в прошивке.
13. ~~CIR-1: консольное окно CIR (`GET_CIR` 0x41)~~ → ✅ **ВЫПОЛНЕНО и проверено на
    железе (2026-07-17).** Усечённое окно CIR вокруг first path (`half u8`,
    центрирование по FP делает прошивка). Снимок окна accumulator берётся в ветке
    приёма (RXFCG) **до перевзвода RX** (`dwt_rxenable`) — грабля затирания
    accumulator снята. `firstPath` трактуется как **10.6 fixed-point** (целый индекс
    отсчёта = `firstPath >> 6`; наш DecaDriver хранит сырой FP_INDEX). Ответ:
    заголовок (fp_index/start_index/count) + пары I/Q, лимит `6+count*4 ≤ 255`
    (`half ≤ 30`). Хост v6: команда `cir` с ASCII-псевдографикой и маркером `<<FP`.
    Проверка: `fp_index` в домене отсчётов (746–749), резкий фронт CIR у FP, маркер
    на LDE leading-edge (на 2–3 отсчёта раньше максимума — корректно). Коммит
    `0e1787b`. Спека — `docs/PROTOCOL_SPEC.md` §8.
14. ~~GUI-детектор присутствия СШП (`tools/mks_gui.py`)~~ → ✅ **ВЫПОЛНЕНО и
    проверено на железе (2026-07-17).** Графическое приложение (tkinter + matplotlib)
    поверх готовой библиотеки `mks_protocol.py` — **прошивка не менялась**.
    Функции: подключение к COM, INIT, выбор PHY-режима (пресеты **Mode 1–8** из
    EVK `dw_main.c` `chConfig` + ручной ввод) через `SET_PHY_CONFIG`, старт/стоп
    детекции (`rx_start`/`rx_stop`), крупный **индикатор присутствия** (кадр за
    последнюю секунду → «СШП В КАНАЛЕ: ЕСТЬ»), телеметрия (count, кадров/с,
    SNR/RSSI/FP_POWER, FP_INDEX), **живой график CIR** с маркером FP и сохранением
    PNG. Опрос платы — в фоновом потоке, отрисовка — в главном (окно не виснет).
    Проверка (Windows, плата МКС + киты EVK1000, Mode 3): лампа реагирует корректно
    (ЕСТЬ на совпадающем режиме, НЕТ на несовпадающем/без сигнала), переключение
    режимов работает; телеметрия физична (count ~15000, 9.2 кадр/с, SNR 0.65 dB,
    RSSI −78.85 dBm, FP_POWER −82.99 dBm, FP_INDEX 752); CIR-график физически верен
    (шумовой пол → резкий фронт у FP, маркер на LDE leading-edge, хвост многолучёвки),
    совпадает с консольным `cir`; конфиг-кнопки блокируются на время детекции, смена
    режима без ошибок порта, сохранение PNG работает. Коммит `77dd412`.
    - Детектор в этой версии **простой** (факт присутствия на выбранном режиме);
      пороговый детектор с P_false, автосканер по режимам и потоковый CIR/водопад —
      будущие шаги.
15. ~~Запись данных экспериментов в CSV (GUI)~~ → ✅ **ВЫПОЛНЕНО и проверено на
    железе (2026-07-17).** Переключатель **Лёгкий / Полный** в `tools/mks_gui.py`.
    Лёгкий → `*_metrics.csv` (14 колонок: метрики на уникальный кадр). Полный →
    `*_metrics.csv` + `*_cir.csv`, нормализованная связь по `frame_id` (форма CIR
    по отсчётам). Данные — из существующего фонового потока опроса (второго опроса
    порта нет); запись по **уникальным кадрам** (рост `count`); файловый I/O в
    фоновом потоке; имена файлов с датой-временем. Формат дружественен к
    MATLAB/pandas (прямоугольные таблицы, невалидные поля → пустая ячейка/NaN).
    **Прошивка не менялась.** Проверка на реальных файлах: лёгкий — 31 кадр,
    `count` 15911..15969, дублей нет, метрики физичны (RXPACC ~304–337,
    RXPACC_NOSAT ~1024–1046, RSSI ~−81 dBm, SNR ~−1.7 dB), `mode` с запятыми
    экранирован; полный — 957 строк CIR = 29 кадров × 33 отсчёта, связь по
    `frame_id` работает, I/Q знаковые int16. Согласованность доменов подтверждена:
    `FP_INDEX` в metrics — сырой 10.6 fixed-point (~48059), `sample_index` в cir —
    в отсчётах (~750); `48059>>6 ≈ 750`. Коммит `f75dd57`.
    - **Заметка (косметика, не баг):** `frames_per_sec` в metrics писался как 0.00
      при медленной записи; частоту всегда можно пересчитать из `timestamp`
      постфактум — на данные не влияет. Поправить при случае.
16. ~~CIR-2a: потоковый режим (`SET_STREAM_MODE` 0x42) + нагрузочный замер~~ →
    ✅ **ВЫПОЛНЕНО и проверено на железе (2026-07-17, прошивка `58a65e3`).** Поток
    работает: по команде плата после каждого принятого кадра сама шлёт свой кадр
    (SMARK 0xDECA | LEN16 | SEQ | DROPPED | CONTENT | метрики30[+окноCIR] | CRC).
    Нагрузочный замер (loopback M1→M2 на ОДНОЙ плате МКС, киты EVK выключены;
    приёмник `tools/mks_stream_probe.py`):

    | Период TX | content=1 (метрики+CIR ~168 Б) | content=2 (метрики ~30 Б) |
    |---|---|---|
    | 50 мс | 19.5 fps | 19.6 fps |
    | 20 мс | 46.9 fps | 47.5 fps |
    | 10 мс | 90.0 fps | 90.6 fps |
    | 5 мс  | 150.7 fps | 165.7 fps |

    `DROPPED(fw)=0` на ВСЕХ режимах. Потери на хосте/CRC: content=2 — везде 0;
    content=1 — единичные (~0.4% на предельном 5 мс).

    **Выводы:**
    1. **Потолок держит НЕ USB CDC и НЕ размер кадра:** лёгкий поток (в 5.5× меньше
       байт) даёт почти тот же FPS, что тяжёлый (разница только на 5 мс: 150.7 vs
       165.7, ~10%). Узкое место — **темп прихода кадров** (TX-периодика + время
       радио-приёма Mode 3): FPS ≈ 1000/период_мс.
    2. **USB CDC на F411 держит с запасом:** и 165 fps×30 Б, и 150 fps×168 Б
       (~25 КБ/с) — без единого дропа прошивкой. Для макета-обнаружителя: CDC тянет
       высокий темп, реальное ограничение будет в радио.
    3. Потери на хосте ~0.4% только на тяжёлом предельном (больше чтения в Python);
       на лёгком — 0. Принято как норма (в CIR-2b — управление скоростью потока).
    4. Замер снят в loopback на ОДНОЙ плате (M1 TX и M2 RX под одним ЦП и одной SPI) —
       TX и RX конкурируют за процессор, поэтому «потолок по TX» частично артефакт
       совмещения. **Подтверждено чистым замером на раздельных платах (см. ниже):**
       на разных ЦП — стабильные 200 fps без потерь, прежний «~150 с потерями» был
       именно артефактом совмещения TX+RX на одном ядре.
    - **Заметка (рассинхрон прошивки, не баг кода):** первый прогон content=1
      снят при СТАРОЙ прошивке на плате (`107338c`) — метрики там сдвинуты на 1 байт
      (нет CONTENT-байта), но FPS-цифры валидны (считаются по маркеру кадра, не по
      содержимому). content=2 на старой прошивке давал 0 кадров (`SET_STREAM_MODE 2`
      → `INVALID_PARAM`, режим 2 в старом билде не поддерживался). Код был верный;
      причина — рассинхрон прошивки платы и хоста, устранено перепрошивкой `58a65e3`.
    - ✅ **Чистый замер потолка USB — ВЫПОЛНЕНО на железе (2026-07-20, две
      независимые платы).** Плата 2 (передатчик, отдельное питание): Mode 4,
      `txperiodic 5 мс`, молотит в эфир автономно. Плата 1 (приёмник, COM3):
      принимает + стримит в USB, probe `--mode 4`. Разные ЦП → приёмник НЕ тратит
      такты на собственный TX (в отличие от loopback на одной плате). Результаты
      (мгновенный FPS по секундным строкам, стабильно):
      - **content=2** (метрики ~30 Б): **199.7 fps**, DROPPED=0, host_lost=0,
        crcErr=0 (4590 кадров за 23 c).
      - **content=1** (метрики+CIR ~168 Б): **199.9 fps** в каждой секунде,
        DROPPED=0, host_lost=0, crcErr=0; fp_index валиден (739–749), CIR в потоке.
      - Метрики физичнее прежних (Mode 4 чище): SNR +5…+6.8 dB, RSSI ~−73 dBm.

      **Итог замера:** транспорт (радио-приём M2 + USB CDC + хост) держит **≥200 fps
      × 168 Б (~33 КБ/с) БЕЗ единой потери**. Разница лёгкий/тяжёлый исчезла (оба
      200 fps) → **USB имеет запас даже на полном потоке метрики+CIR**; узкое место —
      темп передатчика (`txperiodic` минимум 5 мс = 200/с), а НЕ транспорт. Реальный
      потолок USB/приёма **ВЫШЕ 200 fps** — не достигнут (упёрлись в TX).
      **Важно для проектирования макета-обнаружителя:** CDC на F411 гарантированно
      тянет 200 кадров/с полной телеметрии (метрики+CIR) без потерь — запас более чем
      достаточен.
    - **Вход в CIR-2b:** механизма ограничения темпа потока (throttle) в прошивке
      НЕТ — `PROTOCOL_SendStreamFrame` зовётся на каждый RXFCG без пейсинга. В CIR-2b
      добавить с нуля (по образцу `TX_PERIODIC`: интервал по `HAL_GetTick()` или
      «1 из K кадров»), чтобы исследователь выбирал предельный/умеренный режим.
17. ~~Рантайм-обнаружение живых DW-модулей (INIT) + расширенный GET_STATUS~~ →
    ✅ **ВЫПОЛНЕНО и проверено на железе (двухмодульная плата, 2026-07-20, регресс
    пройден).** Коммит `4f205b8`. INIT обнаруживает живые модули по DEV_ID
    (`dwt_readdevid == DWT_DEVICE_ID` ДО тяжёлого `dwt_initialise`), пропускает
    отсутствующие (не роняет общий статус): `OK` + DATA `[live_count, live_mask]`
    если жив ≥1, иначе `RADIO_ERROR`. `GET_STATUS` — первые 7 байт прежние + в конец
    `live_count/live_mask/dev_id[i]` (17 байт). `SET_PHY_CONFIG` конфигурит только
    живые; `RX_STOP/TX_STOP` проверяют `.initialized`. Логика обобщена под
    `DW_DEVICE_COUNT` — **основа для многомодульности будущего 5-канального макета**.
    - **Регресс на двухмодульной — чисто:** `status` → live_count=2, live_mask=0x03,
      dev_ids оба `0xDECA0130`; `init/mode3/rxstart` → OK, GUI работает; `metrics` —
      приём цел, строгий расчёт физичен (RSSI −79.68 dBm, FP_POWER −81.43, SNR −0.18,
      diff ~1.75 дБ → LOS, FP_INDEX сырой 48047 ÷64 ≈ 750); `cir` — эталонная форма
      (fp_index 750, фронт 1990→13086→19275 за 3 отсчёта, хвост многолучёвки),
      идентично прежним прогонам. Новый INIT ничего не сломал на полной плате.
    - **⚠️ Одномодульная плата (только M2) — ОТЛОЖЕНА, не дорешана.** После
      перепрошивки `init` → `RADIO_ERROR`, `status` → live_count=0, mask=0x00, dev_ids
      оба `0x00000000` (M2 прочитался как dev_id=0, не `0xDECA0130`) — хотя ранее
      самая первая DEV_ID-диагностика на этой плате читала валидный ID. Причина не
      установлена. Кандидаты (не подтверждены): `DWT_NUM_DW_DEV` в build-конфиге
      CubeIDE (если не =2, `dwt_setlocaldataptr(1)` для M2 падает в
      `deca_port_select_device` до чтения dev_id); SPI3-init M2 (прескалер
      `spi_set_slow` в `deca_port.c` помечен «подобрать»); рассинхрон прошивки.
      Плату отложили осознанно — двухмодульную используем как передатчик, к
      одномодульной вернёмся позже.
    - Полноценная многомодульность (5 каналов, динамическое обнаружение и роли) —
      задача будущего 5-канального макета-обнаружителя; текущая правка INIT — её основа.
18. ~~Исследовательский GUI «МКС для MATLAB» (`tools/mks_gui.py`, Шаги 1–4)~~ →
    ✅ **ЗАВЕРШЁН и проверен на железе (2026-07-20).** GUI переработан из простого
    поллингового детектора присутствия (п.14, `77dd412`) в полноценный
    **исследовательский инструмент** поверх потокового режима — **логика прошивки
    в этих шагах не менялась** (хост + два малых прошивочных сброса состояния, см.
    ниже). Коммиты по шагам:
    - **Шаг 1** (`45f0387`): приём потока в приложение (не поллинг), кружок-индикатор,
      гашение по таймауту, запись из потока, буфер водопада; общий парсер потока
      вынесен в `tools/mks_stream.py` (один код для GUI и `mks_stream_probe.py`).
    - **Шаг 2** (`c4a0f67`): вкладки, две кнопки Старт/Стоп, **сохраняемые сценарии
      старта** (текстовый мини-язык: init/setphy/mode/rxstart/rxstop/stream/txstop),
      переключатель content (1=метрики+CIR, 2=только метрики).
    - **Шаг 3** (`c9d7377`): **водопад CIR** (matplotlib `imshow`, свежее сверху,
      автомасштаб, colorbar; выравнивание по **абсолютному sample_index** — честно
      показывает дрожание FP по X); порядок вкладок Монитор/Водопад/Настройки.
    - **Шаг 4** (`4acfb14`): **замороженная инфо-панель** над вкладками (Включить/
      Выключить, Старт/Стоп, Пуск TX/Стоп TX, кружок, вся телеметрия — видны на
      ЛЮБОЙ вкладке); **передатчик M1** для self-contained loopback (кнопки +
      команда `txperiodic <мс> [payload hex]` в языке сценария); **водопад
      фиксированной глубины** (поле «Глубина», деф. 120, 20..500 — постоянная высота,
      старые кадры уходят снизу).
    - **Ключевые свойства (проверены на железе):** работает и как **пассивный
      приёмник эфира** (слушать киты EVK / плату-передатчик), и как **self-contained
      loopback M1→M2** прямо из GUI (контролируемый источник без внешних плат);
      **запись потока в CSV** (`metrics.csv` + `cir.csv`, связь по `frame_id` —
      MATLAB-дружественно); **три страховки** от залипшего потока (flush+stream0 при
      подключении/остановке/закрытии); TX-команда при активном потоке исполняется
      **в потоковом треде** (единый владелец COM-порта — pyserial не потокобезопасен
      для конкурентного чтения).
    - **Сопутствующие прошивочные правки (проверены на железе):** сброс залипшего
      снимка CIR/метрик при `RX_START` и `INIT` (`11f7ee4`); сброс `stream_active`
      при `INIT` (`0cf4bf7`) — чтобы новая сессия не показывала данные прошлой.
    - **Значение:** это окончательно переводит роль МКС из «обнаружитель» в
      **исследовательский инструмент для MATLAB** (сбор данных потоком + визуализация
      CIR/водопад + контролируемый источник loopback), как зафиксировано в §1.
      Продуктовый детектор — отдельное будущее 5-канальное устройство.
19. ~~Линия «МКС для MATLAB»: шаблоны для исследователей + удалённый доступ~~ →
    ✅ **ЗАВЕРШЕНО и проверено на железе + VPN (2026-07-20).** Набор **MATLAB-кода
    для исследователей** и **COM-TCP мост** для удалённой работы — **только хост,
    прошивка/протокол не менялись** (байт-в-байт по `docs/PROTOCOL_SPEC.md` v1.5).
    Файлы в `tools/matlab/`:
    - `mks_protocol.m` — библиотека (класс): framing/CRC8, все команды, парсер
      потоковых кадров, парсеры метрик (30 Б) и CIR. **Транспорт абстрагирован**
      (serial COM или TCP); автоопределение serial API (`serialport` R2019b+ с
      откатом на legacy `serial()`).
    - `example_commands.m` — команды + разбор метрик/CIR.
    - `example_stream.m` — **главный шаблон**: приём потока + живой график CIR
      (детекцию/анализ исследователь строит поверх `f.metrics`/`f.cir`).
    - `example_read_csv.m` — офлайн-анализ записанных GUI CSV (`metrics`+`cir` по
      `frame_id`) + водопад, без платы.
    - `mks_tcp_bridge.py` — прозрачный (байты в обе стороны, без разбора протокола)
      COM↔TCP мост: запускается на локальной машине с платой, отдаёт порт по TCP.
    - `README_MATLAB.md` — инструкция по набору + конвенция авторства.
    - **Проверено на боевой схеме (end-to-end, 2026-07-20):** локальная Windows
      (плата COM3, WireGuard `10.8.0.6`) → мост `10.8.0.6:5555` → сервер **Астра
      Линукс** (`10.8.0.2`) по **WireGuard VPN** → **MATLAB 2025** (`serialport`).
      Подтверждено: `ping` OK; `init` с обнаружением модулей (`live_count`/`mask`);
      `set_phy` Mode 3; метрики физичны и разобраны верно (RSSI/FP_POWER/SNR/FP_INDEX,
      LOS-класс); одиночный CIR; потоковый приём (вручную и через `example_stream.m`
      с живым графиком). Разбор метрик/CIR — **байт-в-байт** с прошивкой.
    - **Авторство** в MATLAB-коде — по зонам ответственности из Python-скриптов
      (Wagan — ядро/протокол/поток/CIR; Andrey — оценка мощности и LOS/NLOS;
      Sergey — SET_TX_POWER; Dima — дефолт Mode 3 и мост/проброс), формат единый.
    - **Значение:** закрывает роль МКС как **инструмента для MATLAB-исследователей** —
      сбор данных потоком + удалённый доступ (MATLAB на сервере через VPN). Вместе с
      DOCX-руководством по эксплуатации и GUI (п.18) исследовательская линия готова.
20. **ТЕКУЩЕЕ / остаток фронтов (направления НИР):**
    - **CIR-2b — throttle потока** (по запросу исследователей): в прошивке НЕТ
      пейсинга — `PROTOCOL_SendStreamFrame` зовётся на каждый RXFCG. Добавить
      ограничение темпа (интервал по `HAL_GetTick()` или «1 из K кадров»), чтобы
      выбирать предельный/умеренный режим (см. п.16 «Вход в CIR-2b»).
    - **Режим сырого мониторинга аккумулятора** (без ожидания флага `RXFCG`) — для
      будущего обнаружителя: непрерывный съём CIR без «зачёта» кадра приёмником.
      **Задача обнаружителя, НЕ МКС** (см. §1); водопад шума без сигнала — там же.
    - **Одномодульная плата** (только M2) — разбор `dev_id=0` после перепрошивки
      (кандидаты: `DWT_NUM_DW_DEV`, SPI3-init/прескалер, рассинхрон; см. п.17).
    - **5-канальный макет-обнаружитель** — отдельное устройство (5× DWM1000,
      двухъядерный ЦП + экран); текущая многомодульная логика INIT (п.17) — его основа.
    - Прочее (инфраструктура): **EXTI** (перекрёстные IRQ) вместо polling;
      **автосканер по PHY-режимам**. Далее — эксперименты и автоматизация (этапы ТЗ).
21. ~~§15.1 Канал данных над UWB — стадии (0) и (1)~~ → ✅ **СДЕЛАНО и проверено на
    железе (2026-07-22).** Первая веха дорожной карты §15 (педагогические
    демонстраторы): прикладной слой поверх радио-транспорта.
    - **Стадия (0) — фундамент `content=3`** (тело принятого кадра наружу на хост):
      - разведка `5d3f45a` (2026-07-21, `docs\RECON_data_channel_stage0.md`);
      - **прошивка `14450e6`** (2026-07-22, `App\protocol\protocol.c`, +36/−16):
        `SET_STREAM_MODE` порог `2→3`; `PROTOCOL_SendStreamFrame` — блок метрик стал
        **условным** (content 1/2), добавлена ветка `content=3` (PAYLOAD =
        `data_len u16 LE` + тело `rx_frame` **без FCS**, `frame_len−2`; пропуск при
        `frame_len<2` или `>128`). Команды/`.h` и `Drivers\` **не тронуты**;
      - хост `a344fec` (`tools\mks_data_probe.py` создан; `mks_protocol.py`/
        `mks_stream.py` — разбор `content=3`); доки `799fd2c` (SPEC §8 `content=3` + TASK).
      - **Приёмка на железе:** loopback M1→M2, данные **байт-в-байт**, 321 кадр
        OK=321 / MISMATCH=0, `DROPPED(fw)=0`; регресс `content=1/2` цел (метрики+CIR
        физичны).
    - **Стадия (1) — две радиостанции + фрагментация >119 Б** — **ЦЕЛИКОМ хостовая,
      прошивка НЕ менялась** (транспорта `content=3` достаточно):
      - разведка `5746e12` (`docs\RECON_data_channel_stage1.md`);
      - **логика `eb82814`** (`tools\mks_msg.py`): заголовок **6 Б**
        `src_id|msg_id|frag_idx|frag_cnt|msg_type|flags`, данные **119 Б/фрагмент**,
        `msg_type` TEXT=0/BEACON=1/CALL=2, фрагментация + реассемблер по паре
        **(src_id, msg_id)**, эхо-фильтр по `src_id`;
      - **станция `1b78ea1`** (`tools\mks_station.py`): симметричная (RX `content=3` +
        `TX_FRAME` в ОДНОМ треде-владельце порта), эхо-фильтр, чат с фрагментацией,
        BEACON 1 с / индикатор связи по чужому `src` за 3 с, CALL ×3 с алертом,
        `--loopback`; доки `acc7775` (TASK).
      - **Приёмка на железе:** две платы / две машины — двусторонний чат **без
        само-эха** (эхо-кадры отброшены, нераспозн=0), индикатор работает как
        **измеритель рабочей зоны** (гаснет при уходе за зону слышимости, загорается
        при возврате).
    - Продолжение направления — в §15.1 (RSSI-в-beacon → GUI-станция → этап 2 ARQ).
22. ~~Исследование дальности связи §15.1~~ → ⛔ **ЗАКРЫТО инженерным выводом
    (2026-07-22): ограничение АППАРАТНОЕ** (вероятно, модуль-клон DWM1000-SMA YCHIOT +
    RF-разводка). Симптом: линк плата↔плата ~2 м (RSSI −91…−94 dBm на 2 м, обрывы),
    эталон EVK1000 на том же столе — ~8.5 м. Все программные гипотезы проверены и
    **опровергнуты замером** (коммиты, все 2026-07-22):
    - **TX-калибровка** — `dwt_configuretxrf` звалась только ручной SET_TX_POWER; авто-
      калибровка добавлена в SET_PHY_CONFIG (`986c34c`, разведка `394b6c5`, доки
      `b5e9838`) → помогло слабо.
    - **TX-мощность + Table 20** — ползунок/пресеты, значения по UM §7.2.31.4 Table 20
      manual (ch2/PRF64 `0x67676767`; дефолт `0x0E080222` был ch5-smart — исправлен)
      (`a433190`, `1ebcf18`, доки `a6d1ffb`) → двигает RSSI, не стабильность.
    - **RX-регистры UM §2.5.5** — проверены, `dwt_configure` ставит всё верно под Mode 3
      (разведка `8d64f4d`) → ничего не выпало.
    - **Горячий путь RX** — CaptureCIR условно + ранний `dwt_rxenable` (`0faba66`, PLAN
      `b365215`, доки `635f792`) → не помогло.
    - **Однокабельный полудуплекс `MKS_SIMPLEX`** (один модуль M2, второй выключен) —
      самый чистый тест (`ad0fab7`, `1d61f62`, разведка `5e9399b`, доки `8633d7a`) →
      **гипотеза самоглушения опровергнута** (`6faa8de`).
    - **Приёмный тракт доказан исправным:** наша плата ловит EVK на 5 м −95 dBm с
      рабочим CIR. Асимметрия: наш TX на 2 м ≈ EVK на 5 м → **излучатель ~8 dB слабее
      EVK**. Причина — TX-физика/модуль, не приём и не самоглушение.
    - **Итог:** ни одна программная правка не подняла уровень к EVK → **вывод
      аппаратный**. Полный разбор и передача инженерам — `docs\FINDINGS_range_investigation.md`.

---

## 12. Исправленный пример CRC8 для MATLAB (выдать заказчику)

Документ протокола содержит **ошибочный** пример: там `crc8()` вызывается от
массива, включающего SYNC (`0xAA 0x55`). По принятому решению (§3 п.1) CRC
считается **только по LEN + CMD_ID + PARAMS**. Корректный вариант:

```matlab
function crc = crc8(data)
% CRC-8, полином 0x07, init 0x00, без рефлексии, без xorout
    crc = uint8(0);
    for k = 1:numel(data)
        crc = bitxor(crc, uint8(data(k)));
        for b = 1:8
            if bitand(crc, 0x80)
                crc = bitand(bitshift(crc,1), 0xFF);
                crc = bitxor(crc, 0x07);
            else
                crc = bitand(bitshift(crc,1), 0xFF);
            end
        end
    end
end

% Пример: SET_TX_POWER(15)
SYNC   = uint8([0xAA 0x55]);
LEN    = uint8(0x02);            % CMD_ID + PARAMS = 2 байта
CMD_ID = uint8(0x11);
PARAMS = uint8(0x0F);

crc_input = [LEN CMD_ID PARAMS]; % <-- БЕЗ SYNC
crc = crc8(crc_input);

packet = [SYNC LEN CMD_ID PARAMS crc];
s = serialport("COM5", 115200);
write(s, packet, "uint8");
```

Прошивка проверяет CRC по тем же байтам (LEN..конец PARAMS). Ответ МКС: CRC по
LEN + STATUS + DATA.

---

## 13. Аппаратные дефекты и уроки сборки

### 13.1 КЗ линии NRST на землю — ✅ РЕШЕНО (блокер снят)

**Статус:** блокер снят. Аппаратный сброс восстановлен, плата работает.

**Симптом:** линия NRST постоянно замкнута на землю (КЗ), микроконтроллер
удерживается в сбросе / аппаратный сброс не работает. Проявлялось **одинаково на
всех трёх платах**.

**Причина:** кнопка **BT1 (KLS7-TS3401)** установлена монтажником с разворотом на
**90°**. У 4-выводной тактовой кнопки при повороте на 90° внутренне-замкнутая пара
контактов попадает на цепи **NRST и GND**, что даёт постоянное КЗ. Это
**систематическая ошибка монтажа**, а не дефект схемы или разводки — схема и
трассировка корректны (одинаковое проявление на всех трёх платах подтверждает
монтажную природу дефекта).

**Решение:** выпаять кнопку BT1 (или переустановить её в правильной ориентации).
После выпайки КЗ исчезло, NRST поднялся к VDD, плата заработала, аппаратный сброс
восстановлен.

**Действия на будущее (урок для сборки):**
- При монтаже следующих партий **контролировать ориентацию BT1**.
- Свериться с трассировкой у **Юрия** при её получении.
- Пометить как урок для сборки (проверка ориентации 4-выводных тактовых кнопок).

---

## 14. Питание и безопасность подключения (КРИТИЧНО)

### 14.1 Три источника питания — правила (по разбору схемы)
Разъём XP1 (внутренний USB к материнке, IDC-10): pin1,2=**VBUS +5В**, pin3,4=D−,
pin5,6=D+, pin7,8,9,10=GND. **VBUS с USB НЕ питает плату** — приходит только на
DA1 (USBLC6-2, ESD-защита). Данные D+/D− → МК (PA11/PA12). Основное питание платы
**VDD 3.3В** формируется отдельно: из слота (VDD3V3) → L1 (феррит) → FU1
(предохранитель) → VDD. То есть USB здесь — только сигнал.

Три возможных источника:
1. **Слот PCI-E** — штатное питание платы 3.3В (в рабочем режиме).
2. **USB материнки (XP1)** — только данные; VBUS на питание платы не идёт.
3. **ST-LINK** — может подавать 3.3В на цель (если заведена линия питания).

**ПРАВИЛА:**
- **Только ОДИН источник 3.3В одновременно.** Слот + ST-LINK-питание вместе =
  конфликт двух источников навстречу. НИКОГДА не подключать оба.
- Для отладки на столе: внешнее 3.3В (или слот) как единственный источник;
  **ST-LINK без питания** — 4 провода (SWDIO/SWCLK/GND[/VTREF-вход]).

### 14.2 Инцидент незапуска ПК (урок)
При конфигурации (плата в слоту + большой ST-LINK/V2 с оставленным питанием +
USB к материнке) **ПК не запускался** — видеокарта не стартовала, POST замирал
после одного писка. Причина: конфликт двух источников 3.3В + КЗ на плате (кнопка
BT1, см. §13) грузили БП материнки, он уходил в защиту.
- Отладку вести **на столе**: плата вне ПК, внешнее питание, ST-LINK без питания.
- Всегда один источник 3.3В. **Довести правило до Юрия и Алексея.**

### 14.3 Режим подключения SWD
Плата подключается по SWD в режиме **Hot Plug + Software reset** (проверено в
STM32CubeProgrammer: Device ID 0x431 = F411, 3.24V). В CubeIDE: Debugger → Reset:
**Software system reset**. После устранения КЗ кнопки (§13) аппаратный сброс
восстановлен, но питание всё равно держать по правилам §14.1.

### 14.4 Симптом отладки errno 10061 (target disconnected) — ✅ РЕШЕНО
При отладке от ST-LINK v2 mini (плата запитана ОТ программатора) связь рвалась
(`Remote communication error, target disconnected, errno 10061`) — **причина
подтверждена: нехватка тока от mini-ST-LINK**, когда МК начинает исполнять код.
С mini-ST-LINK DEV_ID читался как `0xFFFFFFFF`; с более мощным ST-LINK / внешним
питанием 3.3В — читается стабильно `0xDECA0130`. **Решение: внешнее питание платы**
(либо более мощный ST-LINK). Правило одного источника 3.3В (§14.1) соблюдать.

---

## 15. Дорожная карта развития МКС (перспективные направления)

> **Статус: наметки, НЕ задачи в работе** — КРОМЕ §15.1, которое **уже в работе**
> (стадии (0)–(1) сделаны и проверены на железе, см. §11 п.21). Остальное (§15.2
> дальнометрия, §15.3 другие чипы, §15.4 платформы) — перспективные направления для
> **отдельных будущих заходов**. Это НЕ текущий остаток фронтов (§11 п.20 — throttle
> CIR-2b, сырой мониторинг для обнаружителя, одномодульная плата `dev_id=0`,
> 5-канальный макет; он остаётся как есть). Реализацию **каждого** направления
> начинать с **разведки по коду и эталонам** (в первую очередь исходники
> **DecaRanging**), поэтапно, по правилу **no-guessing** — не изобретать то, что у
> Decawave уже реализовано.

### 15.1 Канал передачи данных поверх UWB (педагогические демонстраторы)
Показать, как **прикладной слой ложится на радио-транспорт** DW1000 — от простого
к сложному. Ценность в первую очередь **учебная** (демонстраторы → показ педагогам →
нарезка на лабораторные под учебный план).

> **Статус §15.1: исследовательская часть ЗАВЕРШЕНА; дальность ЗАКРЫТА инженерным
> выводом (аппаратное ограничение, см. §11 п.22 и `docs\FINDINGS_range_investigation.md`).**
> Стадии (0)–(1) сделаны и проверены на железе (2026-07-22, §11 п.21): канал данных
> `content=3`, чат с фрагментацией, RSSI-в-beacon (`content=4`) + GUI-станция.
> Реализация **host-only**, прошивка — прозрачный мост. Прикладной слой — на хосте:
> заголовок **6 Б** (см. §11 п.21), адресация `src_id` (**1..255**; **0 — резерв под
> broadcast**). **Двусторонний обмен на дальность** упирается в аппаратный потолок МКС —
> продолжение целесообразно на другой платформе (см. §15.5).
>
> **Сделано:** этап (0) фундамент; этап (1) текстовый чат + фрагментация между двумя
> станциями; **RSSI-в-beacon** (content=4 «данные+метрики»: прошивка `14450e6`/`0e5be34`,
> станция+GUI `5eddc02`) — GUI-станция с сигнал-шкалой RX_LEVEL.
>
> **Диагностика дальности (2026-07-22).** Полевой замер дал плохую дальность (2–3 м);
> антенный тракт исключён. Разведка (`docs\RECON_tx_power_range.md`, `394b6c5`) нашла
> причину: `TX_POWER`/`PG_DELAY` у станции в **сбросовом дефолте** — штатный 2-й шаг
> DecaWave `dwt_configuretxrf()` зовётся только ручной `SET_TX_POWER`, которую станция
> не вызывает.
> - **Правка 1 (авто-калибровка TX):** `SET_PHY_CONFIG` теперь выполняет 2-й шаг
>   (`dwt_setsmarttxpower(0)` + `dwt_configuretxrf`) на каждом живом модуле. Помогло
>   слабо (RSSI −78→−82, дальность 1.5–2 м) — оказалось, дефолт `0x0E080222` был **ch5
>   SMART** (UM §2.5.5.6), неверный для ch2/manual.
> - **Правка 2 (Table 20 manual + ползунок мощности):** канальный дефолт `TX_POWER`
>   заменён на **UM §7.2.31.4 Table 20 manual** (`tx_power_manual`; ch2/PRF64 =
>   `0x67676767`). GUI-станция получила **ползунок мощности** (level 0..0xDF) + пресеты
>   «Стандарт (под маску) = дефолт» (level `0x98`) и «Железный макс» (`0xDF`, ~9 dB выше
>   маски, лабораторный). `SET_TX_POWER` расширен сырым 4-байтовым форматом (под будущий
>   smart). **Приёмка — замер дальности на «Стандарт»/«Железный макс» на железе (за
>   Ваганом).** См. `docs\TASK_tx_calibration.md`, `docs\TASK_tx_power_slider.md`.
> - **Правка 3 (горячий путь приёма — реальная причина).** TX-мощность оказалась НЕ
>   причиной: связь рвалась на всех уровнях (RECON_rx_tuning `8d64f4d`). Причина —
>   приёмник **выключен на каждом кадре** на время `CaptureCIR` (~250 Б SPI,
>   безусловно) + USB-стрима; `dwt_rxenable` последним → кадры в окне теряются на любой
>   мощности (EVK на том же столе бьёт 5–8 м). **Исправлено** (`PROTOCOL_PollRadio`
>   RXFCG): `dwt_rxenable` перевзводится **до** USB-стрима; `CaptureCIR` — условный
>   (`content=1` или командный режим; для станции `content=2/3/4` пропускается).
>   Нашли/тестировали Wagan/Andrey; TX-настройку довёл Dima. **Приёмка — замер
>   дальности (ожидание: скачок к EVK-подобной 5–8 м).** См. `docs\PLAN_rx_hotpath.md`,
>   `docs\RECON_rx_tuning.md`, `docs\TASK_rx_hotpath.md`.
> - **Правка 4 (заход 1: однокабельная сборка — гипотеза самоглушения).** Горячий путь
>   не хватило; гипотеза: в станционном режиме **собственный M1 (TX) глушит собственный
>   M2 (RX)** на одной плате (−94 dBm на 2 м), тогда как приём от далёкого EVK (5 м) даёт
>   −95 с рабочим CIR. Разведка `RECON_single_module_halfduplex.md` (`5e9399b`): раздельность
>   ролей — compile-time `#define`, но **связки RX↔TX на одном чипе не было**.
>   **Реализована compile-time сборка `MKS_SIMPLEX`** (раскомментировать `#define
>   MKS_SIMPLEX` в `board_config.h`): полудуплекс на ОДНОМ модуле M2 (роли M2=M2), M1
>   заглушён (`dwt_forcetrxoff` в INIT, не конфигурится). Связка: `forcetrxoff→starttx→
>   wait TXFRS→rxenable` в `TX_FRAME`/`PollTx`/`TX_STOP` (под `#if DW_SINGLE_MODULE`;
>   двухмодульная не затронута). Тот же бинарник работает и на **третьей плате (только
>   M2)**. **Заход 1 — минимальная проверка гипотезы, БЕЗ Master/Slave** (устранение
>   коллизий — заход 2). **Приёмка — АСИММЕТРИЧНЫЙ замер:** одна `MKS_SIMPLEX`-плата шлёт
>   (txperiodic), вторая только слушает (как тест с китами) — даёт ли ОДИН модуль
>   дальность к EVK (8.5 м) против прежних 2 м. Метки Wagan/Dima. См.
>   `docs\PLAN_single_module_halfduplex.md`, `docs\TASK_single_module_halfduplex.md`.
>   - **РЕЗУЛЬТАТ (2026-07-22): гипотеза НЕ подтверждена.** Однокабельная сборка
>     (один модуль M2, M1 заглушён) дальность **не улучшила** (~2 м). Самоглушение M1→M2
>     **исключено**. Сборка `MKS_SIMPLEX` остаётся в коде (полезна для одномодульной
>     платы), но причина малой дальности — не в ней.
> - **Что уже ИСКЛЮЧЕНО (измерено/проверено на железе):** (1) уровень TX-мощности
>   (ползунок двигает RSSI, дальность не растёт даже на «железном максе»); (2) приёмные
>   регистры (`dwt_configure` ставит весь §2.5.5 верно под Mode 3, RECON_rx_tuning);
>   (3) горячий путь приёма (ранний `rxenable` — не помог); (4) самоглушение (заход 1).
> - **Ключевая улика (асимметрия TX/RX):** наш **приёмник исправен** — слышит далёкий
>   EVK на 5 м при −95 dBm с рабочим CIR. Но наш **передатчик** на 2 м даёт у своего же
>   приёмника ≈ −94 dBm — т.е. наш TX на 2 м доставляет столько же, сколько EVK на 5 м
>   → **наш излучатель/линк ≈ на ~8 dB слабее EVK** даже на калиброванной мощности.
>   Причина смещается на TX-физику/антенну/частоту, НЕ на приём и не на самоглушение.
> - **⛔ ИТОГ (2026-07-22): исследование дальности ЗАКРЫТО — ограничение АППАРАТНОЕ.**
>   Ни одна программная правка не подняла уровень к EVK; причина — TX-физика/модуль
>   (вероятно, **клон DWM1000-SMA YCHIOT**, не оригинал DecaWave) + RF-разводка. Полный
>   разбор, передача инженерам-аппаратчикам и стратегический вывод — в
>   **`docs\FINDINGS_range_investigation.md`** (см. также §11 п.22). Возможные аппаратные
>   направления: сравнение TX-тракта с EVK (осциллограф/анализатор), XTAL-trim/CFO клона,
>   ревизия RF-разводки/переходников.
> - **`MKS_SIMPLEX` — сборочная опция, по умолчанию ЗАКОММЕНТИРОВАНА** (двухмодульная
>   сборка байт-в-байт как раньше). Сохранена: нужна для третьей платы, где физически
>   только M2 (роль TX перенесена на M2, полудуплекс-связка на одном чипе готова).
>
> **Исследовательская часть §15.1 — ЗАВЕРШЕНА** (стадии 0–1: канал данных + чат +
> RSSI/GUI сделаны и проверены; дальность закрыта инженерным выводом). Возможное
> продолжение (на другой платформе, §15.5 ниже, к обсуждению):
> - **Этап (2) — надёжный канал с ARQ** (нумерация уже есть в заголовке; ACK/повтор) —
>   имеет смысл на платформе с рабочей дальностью.
>
> **Направление на будущее (не начато): раздача адресов по HWID.** Стационарная
> **master-станция** хранит БД `HWID→адрес` и выдаёт адреса по запросу (модель DHCP).
> HWID — из **96-битного UID STM32** (регистр `0x1FFF7A10` на F411). `src_id=0` — под
> broadcast-запрос адреса.

**Архитектурный принцип:** плата = **прозрачный радио-мост**, вся логика прикладного
слоя — **на хостах** (Python на ПК), прошивку трогаем **минимально**.

**Разведка по коду (2026-07-20):** `TX_FRAME` (0x20) есть, payload до **125 байт**
(`TX_FRAME_MAX` = 127 − 2 FCS, ограничение стандартного кадра). Приём **уже читает**
payload принятого кадра в буфер `rx_frame` (`dwt_readrxdata` в `PROTOCOL_PollRadio`),
но **наружу на хост payload пока НЕ отдаётся** (поток шлёт метрики+CIR, не тело
кадра) — это **единственный недостающий фундамент**.

Этапы по нарастающей:
- **(0) Фундамент — сквозная передача payload на хост.** ✅ **ВЫПОЛНЕНО** (`content=3`,
  §11 п.21): тело принятого кадра отдаётся расширением потокового режима.
- **(1) Текстовый чат между двумя МКС** — ✅ **ВЫПОЛНЕНО** (§11 п.21): UTF-8 +
  **фрагментация** (кадр ограничен 119 Б данных, сообщение — нет), две станции без
  само-эха. Историческая заметка ниже упоминала лимит «125 байт» — фактический предел
  данных фрагмента с учётом 6-байтового заголовка = **119 байт**.
- **(2) Надёжный канал** — транспортный слой поверх кадров: нумерация (sequence),
  ACK, повторы (ARQ), восстановление порядка. Требует **двустороннего/полудуплексного
  обмена**. Упрощённый учебный аналог TCP **на живых потерях** — зачем нужен транспорт.
- **(3) Передача звука** — микрофон → Python на ПК → МКС → радио → МКС → Python →
  колонки. Аудио-кодек/захват/воспроизведение — **на хостах, НЕ на STM32**. Подход
  **realtime с jitter-буфером, БЕЗ ARQ**: для звука **потеря пакета лучше задержки**
  (педагогический контраст надёжного (2) и realtime (3) каналов).

Пропускная способность **уже измерена** (§11 п.16: 200 кадров/с × 168 байт без
потерь) — **под текст с запасом**; звук требует расчёта буферизации (битрейт кодека
vs темп радио и допустимая задержка).

**Выбор стека — двухступенчатая педагогика (не «или-или»):**
- **Путь A — свой минимальный протокол** (нумерация/ACK/фрагментация руками):
  нагляднее всего для понимания слоёв, лёгкий, реализуется на **STM32F411**. Остаётся
  на **МКС** как начальный учебный уровень.
- **Путь B — стандартный сетевой стек через туннель** (SLIP/PPP поверх нашего
  байтового канала → настоящий IP/UDP/TCP; текст/звук как обычные сокеты): показывает
  «**UWB как сетевой линк**».
- **Путь C — полный стек 802.15.4 / 6LoWPAN.**
- Пути **B/C тяжелее для F411** → поднимать на **макете-обнаружителе** (двухъядерный
  ЦП, больше ресурсов). Логика: сначала Путь A на МКС (слой руками), затем B/C на
  макете (стандартный стек как продолжение).
- **Модель OSI:** L1 = UWB-радио (готово), L2 = 802.15.4 (частично аппаратно в
  DW1000), L3/L4+ — предмет реализации.

### 15.2 Дальнометрия (роль Tag/Anchor, совместимость с EVK1000)
МКС как **активный участник двусторонней дальнометрии (TWR)** и как **учебный стенд**.
Эталон реализации — исходники **DecaRanging** (TWR-автомат Decawave уже готов,
**портировать по шагам, не изобретать**). Педагогически ценно пройти путь поэтапно,
а не выдать готовый чёрный ящик.

Этапы (маленький проверяемый фундамент → полный TWR):
- **(а) Точные метки времени TX/RX** — аппаратные timestamp DW1000 (разрешение
  ~15 пс). Малый проверяемый фундамент.
- **(б) Отложенная передача** — передать кадр в **заданный момент** (delayed TX).
- **(в) Односторонний обмен** — базовый цикл запрос/ответ с метками.
- **(г) Простой single-sided TWR** — расчёт расстояния по меткам времени полёта.
- **(д) Double-sided TWR как EVK1000** + калибровка **antenna delay** — совместимость
  с китами и точная дистанция.

Переиспользует нашу инфраструктуру (**поток, GUI, MATLAB**) для визуализации меток /
времени полёта / дистанции.

### 15.3 «Услышать» другие UWB-чипы (DW3000 и др.)
Вопрос совместимости приёма. **Физика:** DW3000 и DW1000 пересекаются на **канале 5**.
DW1000 может принять DW3000 **только в режиме совместимости** (старый PHY
IEEE 802.15.4-2011). **Нативный режим DW3000** (802.15.4z, STS) DW1000 **декодировать
не может** — максимум увидит **энергию в канале**, но не содержимое кадра.

Полное «услышать любой UWB» смыкается с **задачей обнаружителя** — через **режим
сырого мониторинга аккумулятора без флага `RXFCG`** (детекция энергии без валидного
кадра; см. §11 п.20 и §1). То есть за пределами роли МКС-инструмента — это функция
будущего обнаружителя.

### 15.4 Распределение направлений по платформам (учебная вертикаль)
Направления §15.1–15.3 естественно раскладываются на две платформы — педагогическая
вертикаль от простого к сложному:
- **МКС (STM32F411) — начальный уровень.** Свой минимальный протокол (Путь A, §15.1),
  базовая дальнометрия (§15.2). Скромное железо здесь **преимущество**: студент видит
  канал данных «на простом контроллере», понимает каждый байт. **Доступный массовый
  инструмент** (можно раздать много плат студентам).
- **Макет-обнаружитель (двухъядерный ЦП, экран с тачскрином) — продвинутый уровень.**
  Стандартные сетевые стеки (Пути B/C), сложная обработка на двух ядрах, **автономная
  визуализация на экране** (спектр/энергия канала, водопад CIR, карта дистанций при
  дальнометрии, статус пакетов — прямо на устройстве, без ПК), плюс его **основная
  задача — обнаружение**. Экран с тачскрином — то, чего нет ни у МКС, ни у EVK1000:
  наглядные интерактивные демонстрации прямо на стенде (нагляднее, чем «смотрите в
  MATLAB на сервере»).
- **Итог:** макет перестаёт быть узкоспециальным обнаружителем — становится
  **флагманской учебно-исследовательской платформой**, где обнаружение — одна из
  функций, а рядом сетевые стеки, дальнометрия и визуализация. Это оправдывает
  сложность его разработки.

### 15.5 Платформа связи текст/голос/видео на дальность (другое железо, к обсуждению)
**Основание:** исследование дальности §15.1 показало **аппаратный потолок МКС**
(вероятно, модуль-клон **DWM1000-SMA YCHIOT** + RF-разводка;
`docs\FINDINGS_range_investigation.md`). МКС остаётся отличным **исследовательским**
инструментом (метрики/CIR/поток/визуализация/MATLAB на близких дистанциях), но
**двусторонний обмен на дальность** лучше строить на другой платформе. Наметки (НЕ в
работе):
- **Более мощный МК** (возможно, с аппаратным аудиокодеком) — под голос/видео и тяжёлый
  прикладной слой (стеки B/C из §15.1, jitter-буфер).
- **Оригинальные DecaWave DWM1000** или новые **Qorvo DWM3000** вместо клона YCHIOT —
  предсказуемые ВЧ-характеристики и дальность.
- Опционально **микро-ПК (Linux)** на конце вместо ноутбука — автономный узел связи.
- Прикладной слой (заголовок/фрагментация/реассемблер/эхо-фильтр из §15.1) **переносим**
  — он платформенно-независимый (host-логика).

---

*Документ будет дополняться по мере поступления данных (MATLAB-скрипт заказчика,
результаты слоя протокола).*
